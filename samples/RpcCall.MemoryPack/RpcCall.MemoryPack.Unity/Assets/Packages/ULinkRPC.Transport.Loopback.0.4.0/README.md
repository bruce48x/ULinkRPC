# ULinkRPC.Transport.Loopback

In-memory loopback transport for local tests in ULinkRPC.

## Install

```bash
dotnet add package ULinkRPC.Transport.Loopback
```

## Includes

- `LoopbackTransport.CreatePair(out client, out server)`
